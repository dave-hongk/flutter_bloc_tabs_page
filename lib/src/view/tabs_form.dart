import 'package:flutter/material.dart';

class TabsForm extends StatelessWidget {
  const TabsForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Tabs Page'),
      ),
      body: Container(),
    );
  }
}
